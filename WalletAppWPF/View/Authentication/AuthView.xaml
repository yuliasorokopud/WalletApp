<UserControl x:Class="WalletAppWPF.View.AuthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WalletAppWPF.View"
             xmlns:walletFolder="clr-namespace:WalletAppWPF.View.WalletFolder"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{ x:Type local:SignInViewModel}">
            <local:SignInView></local:SignInView>
        </DataTemplate>
        <DataTemplate DataType="{ x:Type local:SignUpViewModel}">
            <local:SignUpView></local:SignUpView>
        </DataTemplate>
        <DataTemplate DataType="{ x:Type walletFolder:WalletsViewModel}">
            <local:WalletsView></local:WalletsView>
        </DataTemplate>
        <DataTemplate DataType="{ x:Type walletFolder:WalletCreationViewModel}">
            <local:WalletCreationView></local:WalletCreationView>
        </DataTemplate>
    </UserControl.Resources>
    <ContentControl Content="{Binding CurrentViewModel}"></ContentControl>
</UserControl>
